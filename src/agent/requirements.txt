# LiveKit Voice Agent Requirements
livekit-agents
livekit-plugins-openai
livekit-plugins-elevenlabs
livekit-plugins-silero

# Core dependencies
python-dotenv
aiohttp
numpy

# MCP Integration
websockets  # For WebSocket transport in MCP
jsonschema  # Optional: for validating tool arguments